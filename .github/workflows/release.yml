name: Release

on:
  push:
    tags:
      - 'v*.*.*'

env:
  CARGO_TERM_COLOR: always

jobs:
  # ============================================
  # Build Tauri for Windows
  # ============================================
  build-windows:
    runs-on: windows-latest
    permissions:
      contents: write
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup pnpm
      uses: pnpm/action-setup@v4
      with:
        version: 9
    
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '22'
        cache: 'pnpm'
        cache-dependency-path: platforms/tauri/pnpm-lock.yaml
    
    - name: Setup Rust
      uses: dtolnay/rust-toolchain@stable
    
    - name: Rust cache
      uses: Swatinem/rust-cache@v2
      with:
        workspaces: platforms/tauri/src-tauri -> target
    
    - name: Install frontend dependencies
      working-directory: platforms/tauri
      run: pnpm install --frozen-lockfile
    
    - name: Build Tauri app
      working-directory: platforms/tauri
      run: pnpm tauri build
    
    - name: Extract version
      id: version
      shell: bash
      run: |
        echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT
    
    - name: Upload Release Assets
      uses: softprops/action-gh-release@v1
      with:
        files: |
          platforms/tauri/src-tauri/target/release/bundle/nsis/*.exe
        body: |
          ## 🚀 NetBoozt v${{ steps.version.outputs.VERSION }}

          ### 📥 Instalación

          | Archivo | Descripción |
          |---------|-------------|
          | `NetBoozt_${{ steps.version.outputs.VERSION }}_x64-setup.exe` | Instalador NSIS |

          ### ⚡ Requisitos
          - Windows 10/11 (x64)
          - Permisos de administrador para optimizaciones TCP/IP
          
          ### 📋 Changelog
          Ver [docs/CHANGELOG.md](https://github.com/LOUST-PRO/NetBoozt_InternetUpgrade/blob/main/docs/CHANGELOG.md)

          ### 🔗 Links
          - [Documentación](https://github.com/LOUST-PRO/NetBoozt_InternetUpgrade#readme)
          - [Reportar Issues](https://github.com/LOUST-PRO/NetBoozt_InternetUpgrade/issues)
          - [LOUST](https://www.loust.pro)

          ---
          **Full Changelog**: https://github.com/LOUST-PRO/NetBoozt_InternetUpgrade/compare/v3.0.0...v${{ steps.version.outputs.VERSION }}
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
