[package]
name = "netboozt"
version = "3.1.0"
description = "Network Optimization Tool for Windows"
authors = ["LOUST <opensource@loust.pro>"]
license = "Apache-2.0"
repository = "https://github.com/LOUST-PRO/NetBoozt_InternetUpgrade"
edition = "2021"
rust-version = "1.70"
default-run = "netboozt"

# See more keys at https://doc.rust-lang.org/cargo/reference/manifest.html

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["tray-icon"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tokio = { version = "1.0", features = ["full"] }
thiserror = "1.0"
log = "0.4"
env_logger = "0.10"
chrono = { version = "0.4", features = ["serde"] }
reqwest = { version = "0.11", features = ["stream", "rustls-tls"], default-features = false }
futures-util = "0.3"
lazy_static = "1.4"
tauri-plugin-fs = "2"
tauri-plugin-dialog = "2"
tauri-plugin-shell = "2"
tauri-plugin-notification = "2"
signal-hook = "0.3"

# Windows-specific
[target.'cfg(windows)'.dependencies]
windows = { version = "0.52", features = [
    "Win32_Foundation",
    "Win32_System_Threading",
    "Win32_System_Registry",
    "Win32_NetworkManagement_IpHelper",
    "Win32_Networking_WinSock",
    "Win32_System_Services",
] }
windows-service = "0.6"

[features]
# This feature is used for production builds or when a dev server is not specified
custom-protocol = ["tauri/custom-protocol"]

# Enable this feature to build the Windows service binary:
#   cargo build --release --features windows-service-binary --bin netboozt-service
windows-service-binary = []

[[bin]]
name = "netboozt"
path = "src/main.rs"

[[bin]]
name = "netboozt-headless"
path = "src/bin/netboozt-headless.rs"

[[bin]]
name = "netboozt-service"
path = "src/bin/netboozt-service.rs"
required-features = ["windows-service-binary"]

# To build the Windows service binary:
#   cargo build --release --features windows-service-binary --bin netboozt-service
# The NSIS bundler will NOT include it during normal pnpm tauri build
# (it has required-features, so it's only compiled when that feature is enabled).
